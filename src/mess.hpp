#pragma once

#include <unordered_map>
#include <string>

namespace oiftc
{
	namespace dust
	{
		enum class SIZE
		{
			PARTICLE = 1,
			CLUSTER = 2,
			LUMP = 4,
			PILE = 8,
			DUNE = 16
		};
	}
};
